# MarkdownPad Agent Guide

## プロダクト意図

MarkdownPad は、単体の Markdown ファイルを軽く開き、美しく読みながら
編集できる Windows 優先のデスクトップアプリです。機能範囲はメモ帳の
ように小さく保ちつつ、Markdown の表示品質は Obsidian に近づけます。

このプロジェクトが解く問題は次のとおりです。

- Obsidian は Markdown 表示に優れるが、Vault 管理を前提にしたノート
  アプリであり、単体ファイルを素早く扱う用途には向きにくい。
- VS Code は高機能だが、Markdown を読むだけ・少し直すだけの用途には
  重量級すぎる。
- メモ帳は軽いが、Markdown の美しい表示や一部文法への対応が十分では
  ない。

実装では「軽い」「静か」「ファイル中心」を優先してください。知識管理
アプリ、IDE、プラグインホストへ寄せる判断は避けます。

## 初期技術方針

- デスクトップ基盤: Tauri v2。
- フロントエンド: React、TypeScript、Vite。
- エディタ基盤: CodeMirror 6。
- エディタ内 Markdown 解析: CodeMirror Markdown support と
  `@lezer/markdown` の GFM 拡張。
- 印刷や完全レンダリング用途: `markdown-it` または互換レンダラー。
  危険な HTML はサニタイズする。
- 最初の正式対象 OS: Windows。Tauri により将来の macOS/Linux 対応余地
  は残すが、初期実装を複雑にしない。

Tauri 開発には Rust/Cargo が必要です。初期確認時点のローカル環境では
Node.js と npm は利用可能で、Rust/Cargo は後から導入されました。
導入後、`npm run build` と `cargo check` は通過しています。

## MVP スコープ

### 含める機能

- 単体 Markdown ファイルのオープン、編集、保存、名前を付けて保存。
- タブ表示。
- 新しいタブと新しいウインドウ。
- 既定のライブプレビュー編集モード。
- カーソル行または編集中ブロックだけ Markdown ソースを表示する挙動。
- 検索と置換。
- ズーム。
- 右端で折り返すトグル。
- ステータスバートグル。
- 手動保存を基本とするアプリ内復元スナップショット。
- 起動時のタブ、未保存内容、カーソル位置、スクロール位置、表示設定の
  復元。

### 初期範囲外

- プラグイン機能。
- Vault 管理。
- バックリンク、グラフビュー、ワークスペース索引、ノートデータベース。
- wikilink、埋め込み、callout など Obsidian 固有文法の完全互換。
- ターミナル、Language Server、Git パネル、プロジェクトエクスプローラ
  など IDE 的な機能。
- 明示的な保存操作なしに元ファイルへ自動保存する挙動。

## UX 方針

- 起動時の主画面はエディタそのものにする。ランディングページは作らない。
- メニューはメモ帳並みに簡素な「ファイル」「編集」「表示」に限定する。
- 後続仕様で明示されるまで、ツールバー、サイドバー、コマンドパレットを
  追加しない。
- ファイルダイアログ、メニュー、印刷、ウィンドウ制御はできるだけ
  ネイティブ UI を使う。
- 読みやすさと低ノイズを優先する。
- タブごとの未保存状態を分かりやすく表示する。
- 元ファイルは、ユーザーが「保存」または「名前を付けて保存」を選んだ
  ときだけ変更する。

## メニュー仕様

ネイティブアプリメニューは、仕様変更がない限り次の構造に限定します。

### ファイル

- 新しいタブ
- 新しいウインドウ
- 開く
- 保存
- 名前を付けて保存
- 印刷
- タブを閉じる
- ウィンドウを閉じる
- 終了

### 編集

- 切り取り
- コピー
- 貼り付け
- 検索
- 置換
- 全てを選択

### 表示

- ズーム
  - 拡大
  - 縮小
  - 既定値に戻す
- 右端で折り返す
- ステータスバー

「右端で折り返す」と「ステータスバー」はトグルメニューです。

## ライブプレビュー仕様

既定の編集体験は Obsidian 風のライブプレビューです。

- Markdown は編集中も整形表示される。
- カーソルのある行では、`#`、`-`、`*`、コードフェンス、リンク構文などの
  Markdown ソースを表示する。
- カーソル外の行は Markdown として読みやすく表示する。
- 表、画像、コードブロックなど、行単位の装飾でレイアウトが崩れやすい
  構造は、カーソルが内部にある間はブロック全体をソース表示へ戻す。
- 表、フェンス付きコードブロック、罫線は、カーソル外では整形表示し、
  クリックまたはフォーカス操作でブロック全体を Markdown ソース表示へ
  戻して編集できるようにする。
- `---` はカーソル外では水平罫線として表示し、カーソルがある間は `---`
  のソースを表示する。
- `**太字**` は太字、`*強調*` はイタリックとして表示する。
- 明示的な Markdown リンク `[label](https://example.com)` は青色で表示し、
  マウスオーバーで色を変え、クリック時に外部ブラウザで開く。
- 裸の URL 文字列は、明示的な Markdown リンク構文でない限り、クリック
  可能リンクではなく通常の文字列として扱う。
- `\` による Markdown 記号のエスケープを尊重し、カーソル外では
  バックスラッシュだけを隠してエスケープ後の文字を表示する。
- Markdown 構文に含まれない記号や、1バイト/2バイトの短い入力は通常の
  文字列として保持し、改行やライブプレビュー更新で消してはいけない。
- 箇条書きと番号付きリストでは、`Enter` は次のリスト項目を作る。空の
  リスト項目で `Enter` を押すとリスト構文を消してプレーン行へ抜ける。
  リスト項目内の継続段落は `Shift+Enter` の明示操作でのみ作り、先頭を
  1行目の本文開始位置に揃える。
- IME 入力、選択範囲、Undo/Redo、カーソル移動、キーボードショートカット
  の信頼性を、見た目の装飾より優先する。

## Markdown 互換性

初期対応方言は CommonMark + GitHub Flavored Markdown です。

少なくとも次を扱います。

- 見出し。
- 段落と改行。
- 強調と太字。
- 引用。
- 番号付きリストと箇条書き。
- タスクリスト。
- リンクと画像。
- インラインコードとフェンス付きコードブロック。
- 表。
- 取り消し線。
- 明示的なリンク。裸 URL は通常の文字列として扱い、クリック可能リンクに
  しない。
- 安全に処理できる Raw HTML。

Obsidian 固有拡張は初期バージョンの対象外です。

## 状態とインターフェース

エディタと復元処理は、明示的なフロントエンド状態型として扱います。

```ts
export interface DocumentTab {
  id: string;
  title: string;
  path: string | null;
  content: string;
  dirty: boolean;
  cursor: {
    anchor: number;
    head: number;
  };
  scroll: {
    x: number;
    y: number;
  };
  isUntitled: boolean;
}

export interface EditorSettings {
  zoom: number;
  wordWrap: boolean;
  showStatusBar: boolean;
}

export interface RecoverySnapshot {
  version: number;
  updatedAt: string;
  activeTabId: string | null;
  tabs: DocumentTab[];
  settings: EditorSettings;
}
```

Tauri 側の責務:

- ネイティブメニューの作成と更新。
- メニューイベントのフロントエンド通知。
- ネイティブファイルダイアログ。
- 明示的な保存操作後のファイル書き込み。
- 印刷ブリッジ。
- アプリ内復元データの読み書き。

フロントエンド側の責務:

- タブ状態の管理。
- dirty 状態の追跡。
- CodeMirror エディタ状態の管理。
- ライブプレビュー装飾。
- 検索、置換、選択、ステータスバー、ズーム、折り返し。
- 未保存変更の確認ダイアログを出すタイミングの判断。

## 保存と復元

- 保存は手動保存を基本とする。
- 復元スナップショットは元ファイル横ではなく、アプリデータ領域に保存する。
- バックグラウンドの復元データ書き込みで、既存の Markdown ファイルを
  変更してはいけない。
- 起動時、復元データがあれば前回セッションを復元する。
- 復元データには、開いていたタブ、未保存本文、dirty 状態、カーソル位置、
  スクロール位置、アクティブタブ、ズーム、折り返し、ステータスバー表示
  を含める。
- ディスク上のファイルが前回保存時点から変更されている場合、上書き前に
  明確な競合確認または再読み込み確認を表示する。

## テスト方針

リスクに応じてテストを追加します。MVP では次を優先します。

- メニュー構造とメニューイベントのルーティング。
- 新しいタブ、開く、保存、名前を付けて保存、タブを閉じる、未保存確認。
- 保存操作前に元ファイルを変更しない手動保存モデル。
- 復元スナップショット作成と起動時復元。
- 見出し、リスト、強調、リンク、表、タスクリスト、コードブロックの
  ライブプレビュー表示。
- カーソル行または編集中ブロックでのソース表示。
- 検索と置換。
- 右端で折り返す、ズーム、ステータスバーのトグル。

状態ロジックとレンダリング補助は unit test を優先します。UI ができた後は、
エディタ操作と視覚挙動を integration/browser/Tauri テストで確認します。

## ドキュメント運用

- プロダクト判断は `README.md`、実装者向け判断基準はこのファイルに残す。
- 大きな制約や技術方針を変えるときは、両方のファイルを更新する。
- プラグイン、Vault、IDE 的機能を、明示承認なしにロードマップへ入れない。
